# Figure provenance

- image-distribution.png: original synthetic illustration generated on 2026-09-16 with the built-in image-generation tool. It depicts illustrative samples and is not a real dataset or measured embedding.
- Other SVG figures: original diagrams generated by make_figures.py using the Python standard library.
- Noising uses a procedural RGB landscape and independent Gaussian increments for a 1,000-step Markov chain. Displayed values are clipped only when mapped to RGB; the evolving numerical state is not clipped.
- Schedule curves use beta linearly interpolated from 0.0001 to 0.02 over 1,000 steps.
- The three mixture panels share equally likely clean values -2, 0, and 2. They use signal amplitudes 0.96, 0.8, and 0.1, with noise standard deviation sqrt(1-a²); thin curves include the component weight 1/3 and the dark curve is their sum.
- The score illustration uses the intermediate panel's mixture: N(-1.6, 0.6²), N(0, 0.6²), and N(1.6, 0.6²), each with weight 1/3. Both density and score are evaluated analytically. The article's posterior-weight example observes x=1 in this same mixture.
- The reverse-step schematic holds the mean at zero and compares standard deviations 0.85 and 0.45. Its marked sample at 0.9 illustrates a possible draw, not a measured diffusion trajectory.
- CFG and DiT drawings illustrate algebra and architecture; they are not measurements.
- No trained image model was run for these figures.

## Image-generation prompt

Use case: scientific-educational. Asset type: wide editorial illustration for a technical blog on diffusion models. Create a clean, high-resolution landscape image, approximately 3:2 aspect ratio, on a pure white background: twenty small square photographic image tiles scattered with generous white gaps across the page in loose visual clusters. The tiles are diverse natural photographic samples: cats and dogs at upper left, flowers and birds at upper right, mountains and beaches at bottom left, street scenes and houses at bottom right. Each tile has different content, natural realistic detail, straight rectangular edges, very subtle thin neutral border, no frame captions. The arrangement should suggest a collection of images drawn from a distribution, with denser local groups and blank space between groups, rather than a regular grid. Keep all tiles upright or at extremely slight angles, roughly equal sizes, no overlaps; use tasteful restrained natural colors, no decorative props. Absolutely no axes, no arrows, no text, no letters, no numbers, no symbols, no logos, no watermark. This is an explicitly illustrative collection of synthetic examples, not a measured data embedding. The blog will provide its own explanatory caption. Save an image suitable for use as an original local blog asset.
